- name: test cmd source plugin with stdout and bad command
  hosts: localhost
  sources:
    - ansible.eda.cmd:
        command: bash event_source_cmd/idonotexist.sh
        send_output: true
  rules:
    - name: match cmd event
      condition: event.cmd.status == "Running" and event.cmd.number == 2
      action:
        debug:
          msg: "SUCCESS"
